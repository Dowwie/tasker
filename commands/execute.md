# Execute

Execute tasks from a completed plan using isolated subagents.

Use the orchestrator skill in **Execute Mode**.

Run: `Skill("orchestrator")`

Then follow the **Execute Mode** instructions (phase 6).
