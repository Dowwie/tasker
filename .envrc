# Tasker environment configuration
# Run `direnv allow` to activate

# Go binary path
export TASKER_BINARY="${PWD}/go/bin/tasker"

# Add Go binary to PATH for direct invocation
PATH_add "${PWD}/go/bin"
